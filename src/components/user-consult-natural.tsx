import { Box, Textarea } from "@chakra-ui/react";

export const UserConsultNatural: React.FC = () => {
    return (
        <Box>
            <Textarea cols={50} autoFocus></Textarea>
        </Box>
    ); 
}